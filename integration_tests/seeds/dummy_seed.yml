version: 2

seeds:
  - name: 'dummy_seed'
    description: "table with country data."
    config:
      meta: {
            'system': 'dummy_seed_system'
            , 'granularity': 'dummy_seed_granularity'
        }